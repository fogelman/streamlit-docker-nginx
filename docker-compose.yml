version: "3.0"
services:
  default:
    container_name: default
    build: ./src/default
    volumes:
      - "./src/default:/src"
    ports:
      - "8501:8501"
  static:
    container_name: static
    image: nginx:1.19.6
    ports:
      - "8080:8080"
    volumes:
      - "./default.conf:/etc/nginx/conf.d/default.conf"
